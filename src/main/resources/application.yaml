spring:
  application:
    name: proyecto_so
  datasource:
    url: jdbc:mysql://localhost:3306/proyecto_so?allowPublicKeyRetrieval=true&useSSL=false
    username: donovan_user
    password: 1234
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080